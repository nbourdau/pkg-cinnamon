# This is the only way for cinnamon to find gnome-bluetooth library
# This is the same workaround as the one used by gnome-shell
cinnamon: binary-or-shlib-defines-rpath usr/lib/*/cinnamon/libcinnamon.so /usr/lib/gnome-bluetooth
